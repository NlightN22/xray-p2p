# Термины

- `cidr`: сеть в формате CIDR (например, `10.0.0.0/24`), используется для правил redirect и NAT.
- `host`: DNS-имя или IP-адрес узла/сервера.
- `target`: адрес назначения в виде `host:port` (например, `192.0.2.10:22`), используется для forwarding и dns-forward операций.


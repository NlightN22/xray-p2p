#!/bin/sh /etc/rc.common

START=95
STOP=05

USE_PROCD=1
PROG=/usr/bin/iperf3

start_service() {
    procd_open_instance
    procd_set_param command "$PROG" -s
    procd_set_param respawn
    procd_close_instance
}

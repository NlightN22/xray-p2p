# Extensions Templates

These files are templates for optional operator-managed JSON snippets.

Copy the relevant files into:

- `CONFIG_ROOT/config-client/`
- `CONFIG_ROOT/config-server/`

Keep the filenames unchanged so xp2p can detect and merge them deterministically.

